#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        /*int a,b,c;
        cin>>a>>b>>c;
        int r=max(a,max(b,c));
        if(r==a+b||r==b+c||r==a+c)*/
        int a[3];
        for(int i=0;i<3;i++)
        cin>>a[i];
        sort(a,a+3);
        if(a[2]==a[0]+a[1])
        cout<<"Yes"<<endl;
        else
        cout<<"No"<<endl;
    }
    return 0;
}
